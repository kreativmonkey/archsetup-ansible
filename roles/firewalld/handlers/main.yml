---
# handlers file for firewalld

# --reload, not --complete-reload: the complete variant tears down
# the whole ruleset including established connections.
- name: Reload firewalld
  ansible.builtin.command:
    cmd: firewall-cmd --reload
  changed_when: true
