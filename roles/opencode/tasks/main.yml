---
# tasks file for opencode

- name: Install the language servers opencode drives
  community.general.pacman:
    name: "{{ __opencode_lsp_packages }}"
    state: present
  become: true

- name: Write the opencode configuration
  ansible.builtin.include_tasks: __config/render.yml
