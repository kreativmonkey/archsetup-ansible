---
- name: Setup archlinux installation
  hosts: localhost
  become: true
  roles:
    - common
    #- sshd
    - podman
    - pacman
    #- editor
    #- git
    - udev
    - wireguard
    #- obs-studio
    #- lazyvim
    - nix
    - direnv
    - cups
    - gnome-keyring
    #- headroom
    - ollama
    - opencode
    #- thinkpad_dock
    # - openvpn
